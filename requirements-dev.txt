# Development / CI-only tooling. NOT installed in the production image.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
-r requirements.txt

pytest>=8
pytest-asyncio>=0.23
ruff>=0.4            # lint gate in CI (correctness rules only)
pip-audit>=2.7       # dependency CVE scan in CI

# Browser automation for tests/sandbox_demo_ui_test.py — deliberately NOT
# in requirements.txt (it's excluded from the CI pytest run and only used
# for local/e2e runs). After installing, fetch the browser once:
#   playwright install chromium
playwright>=1.45
